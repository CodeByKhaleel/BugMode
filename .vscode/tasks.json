{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-extension",
      "type": "shell",
      "command": "pnpm --filter @bugmode/core-engine build && pnpm --filter @bugmode/parsers build && pnpm --filter @bugmode/prompt-builder build && pnpm --filter bugmode build",
      "group": { "kind": "build", "isDefault": true },
      "presentation": { "reveal": "silent" },
      "problemMatcher": "$tsc"
    }
  ]
}
